package com.taskmanager.model.enums;

/**
 * Enumeration representing the priority level of a task.
 */
public enum Priority {

    /** High priority level requiring urgent attention. */
    HIGH,

    /** Medium priority level representing standard urgency. */
    MEDIUM,

    /** Low priority level for non-urgent tasks. */
    LOW
}
